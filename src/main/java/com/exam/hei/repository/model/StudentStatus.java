package com.exam.hei.repository.model;

/** Administrative status of a student, mirroring the {@code StudentStatus} enum of doc/api.yml. */
public enum StudentStatus {
  ACTIVE,
  SUSPENDED,
  LEFT
}
